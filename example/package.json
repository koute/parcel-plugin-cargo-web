{
  "name": "example",
  "version": "0.0.0",
  "description": "",
  "dependencies": {
    "parcel-bundler": "^1.5.1",
    "parcel-plugin-cargo-web": "file:.."
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
